<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.senai.sp.jandira.imc_app_b">

    <application
        android:allowBackup="true"
        android:icon="@drawable/peso"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/peso"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.novoUsuarioActivity"></activity>
        <activity android:name=".ui.DashBoardActivity" />
        <activity android:name=".ui.activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity_resultado_ncd" />
        <activity android:name=".ui.NcdActivity" />
        <activity android:name=".ui.ResultadoImcActivity" />
        <activity android:name=".ui.ImcActivity" />
        <activity android:name=".ui.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>